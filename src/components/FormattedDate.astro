---
import { dateFormatter } from "@/utils/date-format.helper";

interface Props {
  date: Date;
  class?: string[] | string;
}

const { date, class: className } = Astro.props;
---

<time datetime={date.toISOString()} class:list={className}>
  {dateFormatter(date)}
</time>
